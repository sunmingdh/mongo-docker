version: '3.1'

services:
  mongo:
    image: percona/percona-server-mongodb:5.0-multi
    restart: always
    ports:
      - 27017:27017
  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 8081:8081